// src/api.ts
// Use environment variable if available, otherwise fallback to localhost
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5051';

// You can add more API-related functions here later
